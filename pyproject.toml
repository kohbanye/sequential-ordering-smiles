[project]
name = "sequential-ordering-smiles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["rdkit>=2024.3.6"]

[dependency-groups]
dev = ["mypy>=1.14.0", "ruff>=0.8.4"]

[tool.ruff]
include = ["ALL"]
